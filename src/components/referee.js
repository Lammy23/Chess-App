// import React, { useRef } from "react";
// import { PieceType } from "../context/MovementContext";

export default class referee {
    isValidMove(currentCoordinates, activePieceOrigin, pieceType) {
        console.log("Previous Location: ", {currentCoordinates});
        console.log("Current Location: ", {activePieceOrigin});
        console.log("Piece Type: ", {pieceType});
        
        return true;        
    }
}
    
